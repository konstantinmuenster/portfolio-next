import { styled } from '@config/stitches.config';

export const Overhead = styled('span', {
  fontSize: '$mini',
  lineHeight: '$mini',
  fontWeight: 500,
  textTransform: 'uppercase',
  color: '$secondary900',
});
